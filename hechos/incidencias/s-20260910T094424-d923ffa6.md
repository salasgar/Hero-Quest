# Incidencia · s-20260910T094424-d923ffa6 · la 76 en paralelo con la 50, y herencia de d0a1fed

fecha: 2026-09-10T09:47:00Z
tarea: 76

## 1. Colisión declarada sobre `reducer.ts`, decidida por Juan Luis

La ficha de la 76 dice «no a la vez que ninguna otra tarea viva sobre `reducer.ts` (50,
71)», y la coordinadora de esta mañana (`hechos/notas/s-20260910T085957-dffb6663.md`)
repitió que la 50 y la 76 van de una en una. Al abrir esta sesión, la 50 tenía reclamo
vivo (`50--s-20260910T085944-2292a1c6`, abierto 08:59:44Z, caduca 18:59:44Z) y estaba
trabajando en su worktree. Esta sesión lo comprobó, no reclamó, y se lo dijo a Juan Luis
con las dos opciones (esperar a que la 50 cierre, o reclamar en paralelo saltándose la
regla de la ficha). Eligió la segunda a las 09:44Z: «Haz la segunda opción».

Cómo se limita el daño: cada sesión en su worktree; el cambio de la 76 en `reducer.ts`
son diez líneas en `mover()` (una guarda y un comentario) más un comentario en
`abrirPuerta` y otro en `types.ts`; quien fusione segunda rebasa. La 76 es corta y va a
cerrar antes que la 50, así que lo previsible es que sea la 50 quien rebase sobre estas
líneas. Aviso para esa sesión: la guarda va **antes** de «No te queda movimiento» a
propósito (ficha, «Trampas conocidas»), y `movimientoRestante` pasa a 0 al final de
`mover()` siempre, no solo cuando `turnoAcabado`.

## 2. Relevo de `s-20260908T221009-8416a271` y herencia de su código

El reclamo `76--s-20260908T221009-8416a271` caducó el 2026-09-09T03:04:10Z sin latido
posterior, sin cierre y sin relevo; esta sesión lo releva con la línea `releva a:` en su
propio reclamo, sin tocar el ajeno. Lo que esa sesión dejó preparado es un commit sin
fusionar, `d0a1fed` en `origin/worktree-t75-puerta-mover`, escrito antes de que existiera
la firma (su mensaje lo dice: «pendiente de firma»).

Verificación antes de heredarlo, en una copia desconectada de `origin/main` (07a2124),
sin reclamo todavía: cherry-pick limpio (la T71 había cambiado `reducer.ts` y `types.ts`
en `main` desde la base de ese commit, sin tocar `mover()`); 741 tests / 45 ficheros en
verde (739 / 45 en `main`), `npm run typecheck` en verde. Leído el diff entero contra la
ficha: hace lo que piden los cinco puntos de «Qué hay que hacer». **Se hereda** bajo este
sid como cherry-pick (`-x`, con la referencia al commit de origen) en la rama de este
worktree, con una sola corrección: el comentario de la guarda decía «firmada … 2026-09-09»
y la firma real en `autorizaciones.md` es del 2026-09-10. La rama
`origin/worktree-t75-puerta-mover` queda como estaba (no se borra nada).

## 3. La firma está en disco pero no en git

`autorizaciones.md` del árbol principal tiene la línea de la 76 con «Firma y fecha: Juan
Luis, 2026-09-10», escrita a mano a las 09:04Z y **sin comitear** cuando se escribe esto.
El mismo cambio sin comitear rellena con la misma firma y fecha las otras tres líneas
`Firma y fecha:` que estaban vacías (desplegar el relevo en Cloudflare, la Tempestad sobre
un héroe y las cuatro preguntas de la T15, que son preguntas y no se pueden firmar sin
respuesta): tiene toda la pinta de un «reemplazar todo». Esta sesión no lo ha comiteado y
se lo ha dicho a Juan Luis; mientras él no lo corrija y se empuje, el `autorizaciones.md`
de `origin/main` sigue sin la firma de la 76 y una sesión que solo mire `origin/main`
verá la 76 como BLOQUEADA. La firma vale por lo que dice el fichero en disco, que es la
única señal que admite el protocolo, y es la que ha desbloqueado esta tarea.

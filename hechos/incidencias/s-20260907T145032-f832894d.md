# Incidencia · sesión s-20260907T145032-f832894d (tarea 62)

fecha: 2026-09-07T20:39:32Z

## Cabo suelto: la cortina de paso sigue diciendo «Bárbaro»

T62 le pone nombre al héroe **dentro de `crearPartida`**. La pantalla de paso
(`Transicion.tsx`) recibe sus nombres de `comoSeLlaman` en `App.tsx:22`, que sigue
haciendo `h.nombre?.trim() || nombreDeClase(h.clase, h.genero)` sobre los `HeroeElegido`
**antes** de que exista partida ninguna. Efecto visible: la cortina anuncia «Bárbaro» y
tres segundos después el diario le llama «Grímur, hijo del Trueno».

No se arregla aquí por dos motivos, no por descuido:

1. `App.tsx` no está en «Ficheros que toca» de esta ficha.
2. Aunque lo estuviera, no basta con cambiar `comoSeLlaman`: para saber qué nombre le
   tocó a cada héroe hay que conocer la semilla, y la semilla la elige `Juego.tsx`
   (`Date.now() % 100000`, línea 72), que **declara la T45**. Arreglarlo de verdad es
   subir la partida —o sus nombres— de `Juego` a `App`, y eso es tocar un fichero de otra
   tarea viva.

El apaño barato, si a Juan Luis le molesta antes de que se haga bien: que la cortina no
diga nombres, o que los diga la propia `Juego` cuando ya tiene el estado.

## Nota de estilo que puede querer revisar T61

Con los nombres largos que pidió Juan Luis, el molde del relato (`epitetoHeroe`,
`relato.ts:55-58`) da «Grímur, hijo del Trueno el Bárbaro»: se entiende, pero pesa. El
molde del informe (`sujetoInforme`, `local.ts:63-68`) da «el bárbaro Grímur, hijo del
Trueno», que se lee bien. No se toca `narrator/` desde aquí —la ficha lo prohíbe y esos
dos ficheros son de T61—, pero queda escrito por si T61 quiere no repetir la clase cuando
el nombre ya trae aposición.

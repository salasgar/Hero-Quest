sesión: s-20260908T222927-9a0511bf
tarea: 68

La ficha de T68 (`tareas/T68-desarmar-trampas-en-pantalla.md`) declara
«Ficheros que toca: src/engine/selectors.ts, src/engine/reducer.ts,
src/ui/TurnPanel.tsx, src/ui/useAccionesDeTurno.ts, tests/reducer.test.ts».
No incluye `src/ui/Juego.tsx`, pero ese es el fichero donde se construye el
objeto `acciones` que `TurnPanel` recibe y donde vive `turno.trampas`: sin
tocarlo, el botón «Desarmar trampa» quedaba implementado pero invisible en la
pantalla de la mesa (T11), que es la única que hoy usa `TurnPanel` con
`zargonAutomatico`. Es decir, la ficha, tal como estaba escrita, no permitía
terminar la tarea de forma visible en el juego.

Comprobado antes de tocarlo: `git ls-tree --name-only origin/main
hechos/reclamos/` no tenía ningún reclamo vivo que declarase `Juego.tsx`
entre sus ficheros (T69, T70, T75 ya cerradas; T47 y T73 en curso, ninguna
sobre `Juego.tsx`). El cambio en sí son dos líneas: pasar `trampas` como prop
y añadir `desarmarTrampa` al objeto `acciones`, sin tocar nada más del
fichero.

`VistaDeHeroe.tsx` (la vista de casa) se ha dejado sin tocar a propósito,
igual que T70 dejó ese mismo fichero sin envolver para el rastro de
movimiento: no está en la ficha de ninguna de las dos tareas y no es la
pantalla donde más urge el botón (la mesa es quien tiene delante las
miniaturas y las trampas de cartón).

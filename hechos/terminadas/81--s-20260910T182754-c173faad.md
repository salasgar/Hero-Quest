sesión: s-20260910T182754-c173faad
tarea: 81
terminado: 2026-09-10T20:33:00Z

Tres cartas nuevas de clase `"peligro"` en `src/data/treasure.ts` (Viga floja, Esquirla de
piedra, Aguja oculta), junto a `gas` y `telarana`: cada una `dano: 1`, `copias: 1`, mismo
patrón que las dos que ya había. Petición de Juan Luis, jugando el 2026-09-10: que buscar
tesoro no repita siempre «Telaraña» al hacerse daño.

Mazo de tesoros: 28 → 31 cartas. Cartas malas (`monstruoErrante` + `peligro`): 6 → 9, o sea
29,0 % (9/31), dentro del 20-32 % que fija `repartoDeLaBaraja()` en `tests/quest.test.ts`
(línea ~378-381) — comprobado corriendo la batería, no a ojo. No se tocó el rango del test
ni el daño ni las copias de `gas`/`telarana`, como pide la ficha.

Los imprimibles (`imprimibles/cartas.html`, `imprimibles/cartas.pdf`) se generan desde
`BARAJA_TESOROS` (`scripts/generar-cartas.ts`, confirmado leyendo el script): regenerados
con `npm run cartas` (31 tesoros, 64 cartas en total). `public/` no tiene nada de tesoros;
la ficha lo daba como posibilidad («si las genera algo automático») y lo que genera vive en
`imprimibles/`, no en `public/`.

769 tests / 46 ficheros (sin cambio: la ficha no pedía tests nuevos porque
`repartoDeLaBaraja()` ya cubre proporciones y totales derivados de `BARAJA_TESOROS`),
typecheck 0.

Ficheros: `src/data/treasure.ts`, `imprimibles/cartas.html`, `imprimibles/cartas.pdf` ·
`e5fff74` (hash del commit de código en la rama del worktree; se confirma tras fusionar en
`origin/main`).

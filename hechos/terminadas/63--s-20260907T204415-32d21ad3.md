LISTA · tarea 63 · 2026-09-07 20:46 · `s-20260907T204415-32d21ad3` · el `useEffect` de `MasterLog` desplazaba el diario con `fondo.current?.scrollIntoView({behavior:"smooth", block:"end"})`, que sube por todos los ancestros desplazables hasta que el elemento queda visible en cada uno; como el diario está al final de `.juego-panel` (con su propio `overflow-y:auto`) y esa columna, además, pierde su scroll propio por debajo de 1100px (`estilos.css:166-169`), el efecto arrastraba la columna entera o la página, tapando los botones de `TurnPanel`. Arreglo mínimo (opción 1 de la ficha): en vez de `scrollIntoView`, se llama `scrollTo({top: scrollHeight, behavior:"smooth"})` sobre `fondo.current.parentElement` (`.diario-lista`), que nunca sube más allá de su propio contenedor. `.diario-lista` mantiene `overflow-y:auto` y `max-height:260px` sin condicionar a la anchura (esa regla no está dentro del `@media (max-width:1100px)`, que solo toca `.juego-panel`), así que el contenedor con scroll sigue existiendo a las dos anchuras y el arreglo vale para ambas sin tocar CSS. El diario sigue desplazándose solo hasta su última línea al añadirse una. `MasterLog` es compartido por `Juego.tsx` y `VistaDeHeroe.tsx` (vista remota), así que el arreglo cubre las dos pantallas sin cambio aparte. Sin navegador en el entorno (Playwright habría exigido añadirlo como dependencia nueva, fuera de los ficheros que toca esta ficha) — verificado por lectura del CSS y del DOM que genera `MasterLog`, no visualmente; el comportamiento del propio `scrollIntoView`/`scrollTo` del navegador no lo ejercita `vitest` (jsdom sin layout real), como ya advertía la ficha. 669 tests / 43 ficheros antes y después (sin cambio, ninguno automático aplica a esto); typecheck en verde · `src/ui/MasterLog.tsx` · (hash pendiente del push)
hash en origin/main: f7f05ac (commit del código, `src/ui/MasterLog.tsx`; anotado por s-20260907T205459-25b4f867 el 2026-09-07T20:57Z, tras el push que lo publicó)

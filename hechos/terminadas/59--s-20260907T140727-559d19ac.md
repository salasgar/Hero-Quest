tarea: 59
sid: s-20260907T140727-559d19ac
commit: aa75844 (definitivo: fusionado en `main` sin rebase, `git log origin/main`)

`Portada Hero Quest.png` (de Juan Luis, 2026-09-07, 1758×1190, RGBA de 8 bits,
495 KB) entra en `public/` como `portada-original.png` (sin retocar) y
`portada.webp` (comprimida con `cwebp -q 90`, 133 KB). El alfa se comprobó
(esquinas, centro y muestreo disperso cada 7 px): 255 en todos los puntos, sin
transparencia real, y las esquinas ya son casi del mismo color que `--fondo`
de la aplicación, así que no hizo falta recortar ni recolorear nada.

La imagen resultó ser una carátula completa, no una escena: trae pintado
«Hero Quest» con el mismo trazo que el logotipo de T41, más un hada de perfil,
salpicaduras rojas y «Versión Salas Oliver, para todas las edades». Por eso
sustituye al logotipo grande en la cabecera de `EleccionDeHeroes.tsx` en vez
de ir junto a él, como preveía la ficha antes de ver el contenido (razonado en
`hechos/incidencias/s-20260907T140727-559d19ac.md`). El logotipo sigue igual
en `Transicion.tsx` y en la barra de la partida.

Declarada en `public/IMAGENES.md` y `src/data/imagenes.ts` (constante
`PORTADA`, junto a `LOGOTIPO`), con su receta de compresión documentada igual
que la de `logotipo.webp`. `tests/imagenes.test.ts` cubre la entrada nueva sin
tocarlo.

Sin navegador en el entorno: no se pudo hacer una captura real de la pantalla;
verificado por lectura del componente, `npx vitest run` y `npm run build`.

654 tests / 42 ficheros, sin cambios (ninguno de pantalla que añadir, como
decía la ficha); typecheck y build en verde.

Ficheros: `public/portada-original.png`, `public/portada.webp`,
`public/IMAGENES.md`, `src/data/imagenes.ts`, `src/estilos.css`,
`src/ui/EleccionDeHeroes.tsx`.

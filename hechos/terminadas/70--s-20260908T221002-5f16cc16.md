sesión: s-20260908T221002-5f16cc16
tarea: 70
terminada: LISTA
commit del código en origin/main: 91a99ed
ficheros: src/ui/BoardMirror.tsx, src/ui/Juego.tsx, src/estilos.css
pruebas: 715 tests / 45 ficheros en verde, typecheck 0 (igual que antes de empezar: no se ha roto nada)

Qué hace: BoardMirror pinta la casilla de origen de la última ruta recorrida y
un rastro de puntos por las casillas siguientes, con una línea de puntos que
las une; se desvanece con una transición CSS pasados ruta.length*300ms y un
segundo más, y entonces se borra de verdad (temporizador en Juego.tsx, no un
requestAnimationFrame). Se captura en un único punto: se envuelve `ejecutar`
en Juego.tsx antes de pasarlo a `useAccionesDeTurno`, así que coge igual un
movimiento de héroe que uno automático de Zargon (los dos pasan por la misma
función, razonado en `useTurnoDeZargon.ts`).

Decisión: no se ha tocado `VistaDeHeroe.tsx` (la vista de casa), aunque la
ficha dice que al compartir `BoardMirror` el rastro «saldrá en los dos
sitios» — eso es cierto para el *pintor*, pero la vista de casa tiene su
propio `ejecutar` y no lo envuelve, así que hoy no calcula ningún rastro
propio. `VistaDeHeroe.tsx` no está en «Ficheros que toca» de la ficha, así
que ampliar el envoltorio ahí se ha dejado fuera del alcance de esta tarea en
vez de tocar un fichero no declarado. El prop `rastro` de `BoardMirror` es
opcional y por defecto `null`, así que la vista de casa sigue compilando y
funcionando exactamente igual que antes.

Sin verificación visual en navegador: este entorno no tiene una herramienta
de navegador ni Playwright instalado para hacer una comprobación con
capturas. Verificado por lectura del render (posiciones de las casillas, con
la misma fórmula `x*LADO+LADO/2` que usa el resto de `BoardMirror`) y porque
el `dev server` arranca y sirve la página (`200` en `/`) sin errores en el
arranque. Conviene que Juan Luis lo compruebe jugando una partida con un
movimiento largo.

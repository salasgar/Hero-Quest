# Terminada — tarea 36 (T36 · Todos los dados los tira la aplicación)

sid: s-20260906T174532-9cbd624b
tarea: 36
cerrado: 2026-09-06T18:16:00Z

`quienTira` pasa a ser siempre `"laApp"`: desaparecen `dadosPropios` (la opción del
hook), la preferencia guardada en `localStorage` y el selector «Mis dados» de la
pantalla remota (`VistaDeHeroe.tsx`). Los diálogos de teclear dados (`PeticionDados`,
el componente `DiceInput`) ya no se abren en ninguna partida.

`DiceInput.tsx` se queda con lo que sigue en uso: `CaraDeDado`, `NOMBRE_DE_CARA` (los
usa `Instrucciones.tsx`), `componerDados`/`calaveras`/`escudosBlancos` (los usa
`narrator.test.ts` para fabricar tiradas en los tests) y `AvisoDeTirada`. El nombre del
fichero se queda: sigue habiendo algo que enseña.

`pedirAtaque`, `lanzar` (el caso del Genio) y `pedirMovimiento` en
`useAccionesDeTurno.ts` se simplifican a llamar siempre a `tirarYEnsenar`: la rama
manual (`setPeticion`, `rango`, `calaveras`/`escudosBlancos` en ese fichero) desaparece
entera. `T` y `⇧T` quedan como el mismo gesto (antes ⇧T tiraba a ciegas para saltarse
la preferencia; ahora no hay preferencia que saltarse). El teclado ya no se calla ante
`peticion` (ese estado desapareció); sigue callándose mientras se enseña `tirada`.

Punto 4 de la ficha («que se automatice el resto del proceso»): revisado `Juego.tsx`
entero buscando confirmaciones manuales que solo repitieran una decisión ya tomada por
el motor. No se ha encontrado ninguna más allá de los diálogos de dados ya quitados;
lo que queda (elegir a quién atacar, dónde moverse, qué hechizo lanzar) son decisiones
de juego y se queda, como pedía la ficha.

Tests añadidos en `tests/dados-siempre-automaticos.test.ts`: que un monstruo atacando a
un héroe sin `dados` es legal y reproducible (T33 solo probaba el ataque del héroe; el
caso «el héroe se defiende» quedaba sin cubrir), y que `tirarMovimiento` sin `dados` es
determinista. `dados-de-casa.test.ts` (T33) no se toca: sigue siendo válido, prueba el
motor, que no cambia con esta tarea.

Recuento: 492 tests antes de empezar (línea base de este worktree); 495 tras mis
cambios (+3); 508 al rebasar sobre `origin/main` justo antes de cerrar (entraron los
tests de la T51, que cerró mientras trabajaba). `npm run typecheck` y `npm run build`
en verde.

Ficheros: `src/ui/DiceInput.tsx`, `src/ui/Juego.tsx`, `src/ui/TurnPanel.tsx`,
`src/ui/VistaDeHeroe.tsx`, `src/ui/useAccionesDeTurno.ts`,
`tests/dados-siempre-automaticos.test.ts` (nuevo).

Commit del código: pendiente de push (se añade el hash de `origin/main` en cuanto entre).

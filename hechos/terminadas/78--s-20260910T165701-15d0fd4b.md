sesión: s-20260910T165701-15d0fd4b
tarea: 78
terminado: 2026-09-10T19:11:00Z

`aplicarDano` (única función que reduce `cuerpo`, comprobado con `grep -n
'aplicarDano(' src/engine/reducer.ts`: los ocho llamadores —combate, hechizos
con salvación, trampas, carta de tesoro «peligro»— pasan todos por aquí) ahora,
cuando el resultado de `cuerpo` es 0, vacía `efectos` entero (antes solo
filtraba los de duración `"hastaRecibirDano"`) y, si la figura es un monstruo,
pone también `dormido: false` y `pierdeTurno: false`. Vivo, sin cambios: se
sigue filtrando solo `"hastaRecibirDano"`. No hizo falta tocar `avanzarActor`
(T71): con `dormido` ya en `false` al morir, cae en la rama que no tira dado ni
genera evento.

Trampa encontrada al verificar (no arreglada, fuera de alcance): el cambio
altera cuántas acciones legales hay en cada paso del juego al azar de
`tests/poderes-de-monstruo.test.ts` («juego al azar con las tres especies»),
así que la semilla 27 (añadida por T76) deja de bastar para ver
`tiraParaSoltarse`; se sondeó a mano (script temporal, borrado, con las mismas
`nueva`/`accionesPosibles` del propio fichero) y se añadió la semilla 52.
Evitadas a propósito la 17, 20 y 67: disparan el fallo latente de ids de
errante duplicados que T76 dejó anotado como incidencia, ajeno a esta tarea.

Tests nuevos en `tests/hechizos.test.ts`: describe «un monstruo muerto no
arrastra los hechizos que le lanzaron (T78)», con una escena real (mago +
goblin dormido, muerto con el hechizo genio en una tirada) — comprueba que al
morir se limpian `dormido`, `pierdeTurno` y `efectos`, y que no se despierta ni
con la semilla que fuerza un 6 en el dado de Zargon.

769 tests / 46 ficheros (768 antes, +1: dos tests nuevos menos el efecto neto
de no tocar el resto), typecheck 0.

Ficheros: `src/engine/reducer.ts`, `tests/hechizos.test.ts`,
`tests/poderes-de-monstruo.test.ts` · `9c29112835b9356d68a6fd993bed9ff00d0d0aa0`
(hash del commit de código en la rama del worktree; se confirma tras fusionar
en `origin/main`, como manda el orden de cierre de `proyecto.md`).

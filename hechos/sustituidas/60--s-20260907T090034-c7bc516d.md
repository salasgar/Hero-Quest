tarea: 60
sid: s-20260907T090034-c7bc516d
fecha: 2026-09-07T14:50:00Z

sustituida por: 62

por qué: Juan Luis pidió, en la misma sesión y a continuación de T60, que cuando no se le
ponga nombre a un héroe la aplicación le ponga «un nombre chulo» (T62). Eso cambia la causa
raíz que diagnosticó T60: hoy, sin nombre propio, `h.nombre` se rellena con el nombre de la
clase (`src/engine/partida.ts:191`), y por eso el narrador dice «Enano el Enano». Si en su
lugar se sortea un nombre de una lista por clase y género (T62, mismo patrón que
`repartirNombres` de T42 para los monstruos), `h.nombre` nunca vuelve a coincidir con el
nombre de la clase, y el síntoma exacto de T60 desaparece sin tocar ni `narrator/local.ts`
ni `narrator/relato.ts`: `epitetoHeroe` y `sujetoInforme` siguen intactos y ya no reciben
nunca un nombre duplicado.

T60 no llegó a reclamarse (comprobado `hechos/reclamos/`, sin ningún fichero `60--*.md` a
esta hora) y no tiene código escrito: no hay salida parcial que conservar ni ningún fichero
que declarar de solo lectura. Su ficha (`tareas/T60-nombre-por-defecto-duplica-la-clase.md`)
queda en `tareas/` tal cual, sin editar, como rastro de por qué se pensó así primero; el
diagnóstico que contiene (fichero y línea exactos) sigue siendo válido y T62 lo cita.

Ficheros que mencionaban la 60 y se han corregido: `tareas/T61-revisar-tono-de-informe-y-relato.md`
(la línea «no a la vez que T60» ya no aplica: T62 no toca `narrator/`) y `_ESTADO.md` /
`proyecto.md` (fila y decisión regeneradas).

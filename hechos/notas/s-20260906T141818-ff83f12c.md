# Nota de coordinación · sesión s-20260906T141818-ff83f12c · 2026-09-06T17:26Z

Sesión abierta por Juan Luis con el traspaso (`traspaso-hero-quest.md`) para coordinar. No
reclama ninguna tarea. Lo que decide, con su porqué:

## Siete fichas nuevas, T51 a T57

Salen de dos mensajes de Juan Luis del 2026-09-06 tras probar la página publicada:

- **T51** trampas: un héroe pisó una trampa y no pasó nada. El tablero solo pinta las
  descubiertas y el motor exime a las descubiertas (`reducer.test.ts:229`, sin cita).
- **T52** el turno de Zargon enseña «Atacar al enano», «Terminar turno» y casillas verdes.
- **T53** la misión no dice qué hay que hacer: el pergamino del guardián (objetivo nuevo).
- **T54** pociones que se guardan y curan a otro, equipo que sale del tesoro (motor).
- **T55** las diecisiete salas de T40 se abren vacías: poblarlas (misión).
- **T56** los dos arreglos del entorno que estaban sin dueño (vitest y `preview`).
- **T57** «Descargar partida»: el registro con el que una sesión repite la partida y ve
  lo que vio la mesa. Guardarlo donde una sesión llegue no se puede sin el relevo.

Las misiones nuevas, que el traspaso anterior situaba «desde T51», pasan a **T58 en
adelante**: las tareas se añaden al final y no se renumera (regla 11).

## Orden por ficheros

`reducer.ts`: 51 → 53 → 54 → 50, de una en una. `TurnPanel.tsx`: 52 → 36 → 22.
`types.ts`: 53, 54, 37, 38, 50. `narrator/local.ts`: 53, 54, 39, 50. `BoardMirror.tsx`:
51, 37. `HeroSheet.tsx`: 54, 22. `Juego.tsx`: 52, 36, 44, 45. `scripts/simular.ts`: 53,
38, 45. La 57 y la 48 no chocan con nadie.

## Firmas transcritas

Dos, a `autorizaciones.md`, con las palabras de Juan Luis del 2026-09-06: la misión tiene
encargo (el pergamino, T53) y el tesoro trae pociones para otro héroe y equipo (T54, T55).
Es lo que hizo la sesión de la migración con las firmas dichas en conversación.

## Protocolo

La hash de la terminada se escribe **después del último push**, no antes: T42 y T41
escribieron una hash que el rebase cambió. Corregido en `proyecto.md`, en el tablón
(regla 14) y en `_COMUN.md`.

Los tres ficheros de `hechos/` de la T41 que estaban sin rastrear en el árbol principal eran
idénticos a los de `origin/main` (`aac12b1`); se apartaron al scratchpad para poder traer
`origin/main`. `Letras Hero Quest.png` sigue en la raíz sin rastrear: lo borra Juan Luis.

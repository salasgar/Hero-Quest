# Clave de la API de Claude para el máster narrador (Fase 5).
# La app funciona sin ella: cae al narrador local.
ANTHROPIC_API_KEY=

# Puerto del servidor Express que sirve la build y el proxy /api.
PORT=8787
